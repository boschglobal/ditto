#
# Version marker for the Postgres extension same-release boot self-check (PostgresExtensionVersions).
# component = which of the three layered Postgres extension jars this is; version = the Ditto release it was built from.
# All mounted Postgres extension jars MUST share the same version.
#
component=persistence (ditto-postgres-persistence-extension)
version=${project.version}
